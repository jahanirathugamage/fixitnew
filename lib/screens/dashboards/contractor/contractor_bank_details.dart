import 'package:flutter/material.dart';

class ContractorBankDetails extends StatelessWidget {
  const ContractorBankDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Bank Details")),
      body: const Center(child: Text("Contractor Bank Details Page")),
    );
  }
}
