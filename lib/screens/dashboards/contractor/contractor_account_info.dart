import 'package:flutter/material.dart';

class ContractorAccountInfo extends StatelessWidget {
  const ContractorAccountInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Account Information")),
      body: const Center(child: Text("Contractor Account Info Page")),
    );
  }
}
